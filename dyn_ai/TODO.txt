# dyn_ai.py
- on checks: launch app after 2 seconds
- Add Setup button on regular
  - Make it similar to pre checks.
  - Here and on prechecks: disable setup button until setup closes
- tests:
 - buttons disabled when they must be
 - AIW is correct everytime - no track, different versions of a track...etc

# dyn_ai_setup.py
- cfg.yml ticks are not visible (but they work)
- cfg changes: please restart dyn_ai to apply these changes
- import tab -> option to import from "extra stats=1", better explanation
- tests:
 - data used is updated dynamically
 - vehicles dont match json, add them, save them,then they must match

# dyn_ai_visualizer.py
- tests:
 - data used is updated dynamically

# ALL
- Add logging system to be retrieved by setup
- Are backups enabled by default?
- simulation better tests
  - reallistic laptimes
  - outliers to test limits
- add ai target
- check use of aiRange
- check data(weather? AI level?) from current PLR and add to data
- focused tests
  - test pre-check tests
