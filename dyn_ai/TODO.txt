# TO DO

## Code Improvements

- Add the possibility to import raceresults when the user has extra stats=1
- Add an AI-target: let the user decide if their time should be top 10% of AI laptimes, or bottom 25% or...
- Investigate the use of weather (probably can be gotten from some files as well) to improve data.
  - No weather seems like a good-enough estimation but having more data cannot hurt.
- Investigate the use of the variable aiRange and what it does, it may be interesting to improve this program.
- When opening Setup of Graph/Visualize, the user should know it is loading (maybe disabling the button?)
- Extract hardcoded values - Move DEFAULT_A_VALUE (32.0), ratio bounds (0.3-3.0), b limits (10-200), and plot ranges to configuration file.
- Implement async database operations - Replace blocking SQLite calls with a queue-based system to prevent UI freezes during large imports or formula recalculations.
- Improve outlier detection - Currently outlier detection runs only during auto-fit. Extend to real-time data point addition with configurable sensitivity.
- Add formula versioning - Track formula history per (track, class, session) to allow reverting to previous parameter sets or visualizing how parameters evolved over time.
- Implement session merging - When multiple qualifying sessions occur for same track/class, merge data points intelligently instead of treating each as independent.
- Code should be smaller, Exe files should all be under 50M.

## Feature Additions

- Multi-vehicle per session support - Currently each race data point stores only user's vehicle class. Add ability to track and learn from all AI vehicles in the session.
- Export/import formulas - Add JSON export of all formulas for sharing between users or backing up.
- Track-specific a parameter - Allow a to vary per track rather than global constant, as different tracks have different sensitivity to ratio changes.
- Real-time ratio adjustment - While GTR2 is running, apply ratio changes immediately (some GTR2 versions support hot-reloading AIW files without restart).
- Performance graph overlay - In visualizer, show predicted vs actual lap times as scatter plot with regression line.
- Command-line interface - Add CLI mode for batch processing race results without GUI, useful for server-side or automated testing.
- Per-vehicle ratio multipliers - Allow different ratio offsets for specific AI drivers to simulate skill differences.

## Performance & Reliability

- Add connection pooling for SQLite - Reuse database connections across threads to reduce overhead from frequent connect/disconnect cycles.
- Implement file monitoring fallback - If inotify (Linux) or ReadDirectoryChangesW (Windows) is available, use it instead of polling to reduce latency.
- Add validation for AIW file integrity - Before writing ratio, verify file is not corrupted and contains expected structure. Corrupted AIW files could crash GTR2.
- Implement graceful degradation - If AIW file cannot be found, show warning but continue monitoring (maybe track was deleted). Currently fails the entire autopilot for that session.

## Testing & Documentation

- Test the pre-check tests themselves
- Add more unit tests - Core formula calculations, AIW parsing, data extraction, and outlier detection need test coverage.
- Add "perfect data" to the simulation testing. Data that should produce a Ratio of 0.5, 1.0. 1.5 ...
  - Do this also for the median formula that takes more than one laptime.
- Expand logging - Add debug-level logging for all regex operations, file searches, and database queries to ease troubleshooting.
- Create schema migration system - Database schema changes currently require manual intervention or fresh database. Add versioned migrations.
